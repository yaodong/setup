#!/usr/bin/env bash

# Exit immediately if a command exits with a non-zero status
set -e

ln -sf ~/.local/setup/zsh/.zshrc ~/.zshrc
ln -sf ~/.local/setup/nvim ~/.config/
ln -sf ~/.local/setup/starship/starship.toml ~/.config/starship.toml
ln -sf ~/.local/setup/tmux/.tmux.conf ~/.tmux.conf
ln -sf ~/.local/setup/ghostty ~/.config/ghostty
ln -sf ~/.local/setup/ideavim/ideavimrc ~/.ideavimrc
